{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec node --loader ts-node/esm src/index.ts",
    "build": "npm run clean && npx tsc",
    "start": "node dist/index.js",
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); const deleteFiles = (dir, exts) => { if (fs.existsSync(dir)) fs.readdirSync(dir, {recursive: true, withFileTypes: true}).forEach(f => { if (f.isFile() && exts.some(ext => f.name.endsWith(ext))) fs.unlinkSync(path.join(f.path || f.parentPath, f.name)); }); }; deleteFiles('src', ['.js', '.d.ts', '.js.map', '.d.ts.map']); if (fs.existsSync('dist')) fs.rmSync('dist', {recursive: true, force: true}); console.log('✅ Cleaned compiled files');\"",
    "vercel-build": "npm run build",
    "postinstall": "npx tsc --version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sanity/client": "^7.13.2",
    "@types/nodemailer": "^7.0.3",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2",
    "morgan": "^1.10.1",
    "nodemailer": "^7.0.10",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.10.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2"
  },
  "type": "module"
}
